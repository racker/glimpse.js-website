var config = {
  width: 700,
  height: 250,
  forceY: [0]
};

  myGraph.config('height', 150);
  mygraph.height(150);

  myGraph.config({
    width: 600,
    height: 200
  });

  myComponent.config('color', 'red');
  myComponent.color('green');

  myComponent.config();

